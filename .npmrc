# NPM Configuration for Security

# Audit settings
audit-level=moderate
fund=false
save-exact=true

# Security best practices
ignore-scripts=false

# Package lock
package-lock=true

# Legacy peer deps (only if needed)
# legacy-peer-deps=false
